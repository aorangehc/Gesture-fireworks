Place MediaPipe Hands assets here for offline use.

Expected files (Solutions API, typical names):
- hands.js
- hands_solution_packed_assets.data
- hands_solution_simd_wasm_bin.wasm

All files must reside in this folder so the loader can resolve them via locateFile('./vendor/mediapipe/<file>').

